.DS_Store
node_modules/
.env
# Carpeta de fotos originales del cliente (privadas, no desplegar)
fots del cliente. no tocar/
# Documentos internos de diseño (no necesarios en producción)
DESIGN.md
venv/
# Script local con tokens MCP (no subir a repos)
setup.sh
